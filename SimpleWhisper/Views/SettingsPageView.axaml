<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:SimpleWhisper.ViewModels"
             xmlns:views="using:SimpleWhisper.Views"
             xmlns:icons="clr-namespace:Material.Icons.Avalonia;assembly=Material.Icons.Avalonia"
             x:Class="SimpleWhisper.Views.SettingsPageView"
             x:DataType="vm:SettingsPageViewModel">

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <StackPanel Spacing="28" Margin="24">

            <!-- General -->
            <StackPanel Spacing="8">
                <TextBlock Text="General" FontWeight="SemiBold" FontSize="14" />
                <CheckBox Content="Minimize to system tray when closing"
                          IsChecked="{Binding MinimizeToTray}" />
                <TextBlock Text="The app will continue running in the background and can be restored from the tray icon"
                           FontSize="12"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource SystemControlForegroundBaseMediumLowBrush}" />
            </StackPanel>

            <!-- Recording Mode -->
            <StackPanel Spacing="8">
                <TextBlock Text="Recording Mode" FontWeight="SemiBold" FontSize="14" />
                <RadioButton Content="Hold (push-to-talk)"
                             IsChecked="{Binding IsToggleMode, Converter={x:Static BoolConverters.Not}}" />
                <RadioButton Content="Toggle (press once to start, press again to stop)"
                             IsChecked="{Binding IsToggleMode}" />
            </StackPanel>

            <!-- Performance -->
            <StackPanel Spacing="8">
                <TextBlock Text="Performance" FontWeight="SemiBold" FontSize="14" />
                <CheckBox Content="{Binding GpuAccelerationLabel}"
                          IsChecked="{Binding UseHardwareAcceleration}"
                          IsEnabled="{Binding IsGpuAvailable}" />
                <StackPanel Orientation="Horizontal" Spacing="12"
                            IsVisible="{Binding NeedsRestart}">
                    <TextBlock Text="Restart required to apply"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource SystemControlForegroundBaseMediumLowBrush}" />
                    <Button Content="Restart Now"
                            Command="{Binding RestartAppCommand}"
                            Padding="12,4" />
                </StackPanel>
            </StackPanel>

            <!-- Clipboard -->
            <StackPanel Spacing="8">
                <TextBlock Text="Clipboard" FontWeight="SemiBold" FontSize="14" />
                <CheckBox Content="Copy transcription to clipboard after each recording"
                          IsChecked="{Binding CopyToClipboard}" />
                <CheckBox Content="Show desktop notification after each recording"
                          IsChecked="{Binding ShowNotification}" />
                <CheckBox Content="Paste transcription into focused window"
                          IsChecked="{Binding PasteIntoFocusedWindow}"
                          IsEnabled="{Binding IsPasteAvailable}"
                          ToolTip.Tip="Not available on Wayland."
                          ToolTip.ShowDelay="300"
                          ToolTip.ShowOnDisabled="True" />
            </StackPanel>

            <!-- Hotkey -->
            <StackPanel Spacing="8">
                <TextBlock Text="Global Hotkey" FontWeight="SemiBold" FontSize="14" />
                <views:HotkeyRecorderButton HotkeyText="{Binding HotkeyText}"
                                           IsVisible="{Binding IsHotkeyEditable}" />
                <TextBlock TextWrapping="Wrap"
                           FontSize="12"
                           Foreground="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"
                           IsVisible="{Binding IsHotkeyEditable, Converter={x:Static BoolConverters.Not}}"
                           Text="On Wayland, the shortcut is managed by your compositor. Use the button below to configure it in your system's keyboard shortcuts settings." />
                <Button Content="Open System Shortcuts Settings"
                        Command="{Binding OpenSystemShortcutsCommand}"
                        HorizontalAlignment="Left"
                        IsVisible="{Binding IsHotkeyEditable, Converter={x:Static BoolConverters.Not}}" />
            </StackPanel>

            <!-- Models Directory -->
            <StackPanel Spacing="8">
                <TextBlock Text="Models Directory" FontWeight="SemiBold" FontSize="14" />
                <Grid ColumnDefinitions="*,Auto,Auto">
                    <TextBox Grid.Column="0"
                             Text="{Binding ModelsDirectory}"
                             FontSize="12"
                             FontFamily="Monospace"
                             VerticalAlignment="Center" />
                    <Button Grid.Column="1"
                            Command="{Binding BrowseModelsDirectoryCommand}"
                            Content="Browse…"
                            Margin="6,0,0,0"
                            Padding="10,4"
                            VerticalAlignment="Center" />
                    <Button Grid.Column="2"
                            Command="{Binding OpenModelsDirectoryCommand}"
                            Margin="6,0,0,0"
                            Padding="6,4"
                            VerticalAlignment="Center"
                            ToolTip.Tip="Open in file manager">
                        <icons:MaterialIcon Kind="FolderOpen" Width="16" Height="16" />
                    </Button>
                </Grid>
            </StackPanel>

            <!-- System Info -->
            <StackPanel Spacing="8">
                <TextBlock Text="System Information" FontWeight="SemiBold" FontSize="14" />
                <Grid ColumnDefinitions="160,*" RowDefinitions="Auto,Auto,Auto" >
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Operating System"
                               Foreground="{DynamicResource SystemControlForegroundBaseMediumLowBrush}" />
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding OsDescription}" TextWrapping="Wrap" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Desktop Environment" Margin="0,6,0,0"
                               Foreground="{DynamicResource SystemControlForegroundBaseMediumLowBrush}" />
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding DesktopEnvironment}" Margin="0,6,0,0" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Display Server" Margin="0,6,0,0"
                               Foreground="{DynamicResource SystemControlForegroundBaseMediumLowBrush}" />
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding DisplayServer}" Margin="0,6,0,0" />
                </Grid>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>

</UserControl>
